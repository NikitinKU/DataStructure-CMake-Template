// Copyright 2022 Kirill Nikitin

#include <stdexcept>
#include "../lib_addition/addition.h"

float addition(int a, int b) {
	//if (b == 0) throw std::invalid_argument("Input Error: can't divide by zero!");
	return (a) + b;
}
